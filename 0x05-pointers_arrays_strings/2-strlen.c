#include "main.h"
/**
 *_strlen - returns the length of a string
 *@s: the string to be checked
 * Return: length of the string
 */
int _strlen(char *s)
{
int length;
length = strlen(*s);
return (len);
}
